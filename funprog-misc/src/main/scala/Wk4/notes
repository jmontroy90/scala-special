
def assertAllPos[S <: IntSet](r: S): S = ...

"S <: IntSet" means S must be a subtype of IntSet, ie class S extends IntSet {...}

So this full line reads: function assertAllPos takes an argument r of type S and returns type S, where S MUST be
a subtype of IntSet

Other direction holds as well (supertypes) "S >: IntSet"

Both! Between! [S >: NonEmpty <: IntSet]

Covariant types: if NonEmpty <: IntSet, does it also hold that List[NonEmpty] <: List[IntSet]?